---
name: Functional Tests

on:
  push:
    branches:
      - 'dependabot/**'
      - 'feature-**'
      - 'fix-**'
      - 'main'

jobs:
  positive_tests:
    name: Positive tests
    timeout-minutes: 5
    strategy:
      matrix:
        os: ['ubuntu-latest', 'windows-latest', 'macos-latest']
        from: ['yaml', 'json', 'xml']
        to: ['yaml', 'json', 'xml']
        exclude:
          - from: 'yaml'
            to: 'yaml'
          - from: 'json'
            to: 'json'
          - from: 'xml'
            to: 'xml'
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - name: Prepare
        run: |
          echo "s:" > expected.yaml
          echo "  v: '1'" >> expected.yaml
          echo "  modules:" >> expected.yaml
          echo "    - test1" >> expected.yaml
          echo "    - test2" >> expected.yaml
          echo '{"s":{"v": "1","modules": ["test1","test2"]}}' > expected.json
          echo "<s><v>1</v><modules>test1</modules><modules>test2</modules></s>" > expected.xml
      - name: Convert
        uses: ./
        id: convert
        with:
          path: expected.${{ matrix.from }}
          from: ${{ matrix.from }}
          to: ${{ matrix.to }}
      - name: Save file
        run: echo '${{ steps.convert.outputs.data }}' > actual.${{ matrix.to }}
      - name: Print file
        run: cat actual.${{ matrix.to }}
      - name: Validate
        run: cmp -s "expected.${{ matrix.to }}" "actual.${{ matrix.to }}" || diff "expected.${{ matrix.to }}" "actual.${{ matrix.to }}"
        shell: bash
      - name: Clean
        run: |
          rm -f expected.yaml
          rm -f expected.json
          rm -f expected.xml
          rm -f actual.${{ matrix.to }}
        shell: bash
